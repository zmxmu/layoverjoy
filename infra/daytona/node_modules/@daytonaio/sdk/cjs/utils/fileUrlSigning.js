"use strict";
/*
 * Copyright Daytona Platforms Inc.
 * SPDX-License-Identifier: Apache-2.0
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.computeFileUrlSignature = computeFileUrlSignature;
exports.resolveExpires = resolveExpires;
exports.buildSignedFileUrl = buildSignedFileUrl;
const crypto_1 = require("crypto");
const DaytonaError_1 = require("../errors/DaytonaError");
const SIGNATURE_V1_PREFIX = 'v1_';
const DEFAULT_TTL_SECONDS = 3600;
function computeFileUrlSignature(signingKey, method, path, expires) {
    const canonical = `v1:files:${method}:${path}:${expires}`;
    const digest = (0, crypto_1.createHmac)('sha256', signingKey).update(canonical).digest('base64url');
    return `${SIGNATURE_V1_PREFIX}${digest}`;
}
function resolveExpires(ttlSeconds) {
    if (ttlSeconds === undefined) {
        return Math.floor(Date.now() / 1000) + DEFAULT_TTL_SECONDS;
    }
    if (!Number.isFinite(ttlSeconds)) {
        throw new DaytonaError_1.DaytonaError('ttlSeconds must be a finite number');
    }
    if (ttlSeconds <= 0) {
        return 0;
    }
    return Math.floor(Date.now() / 1000) + Math.floor(ttlSeconds);
}
function buildSignedFileUrl(toolboxProxyUrl, sandboxId, operationPath, method, filePath, signingKey, ttlSeconds) {
    if (!signingKey) {
        throw new DaytonaError_1.DaytonaError('Sandbox signing key is not available. Call refreshData() or fetch the sandbox by ID to load it.');
    }
    if (!toolboxProxyUrl) {
        throw new DaytonaError_1.DaytonaError('Sandbox toolbox proxy URL is not available. Call refreshData() or fetch the sandbox by ID to load it.');
    }
    const expires = resolveExpires(ttlSeconds);
    const signature = computeFileUrlSignature(signingKey, method, filePath, expires);
    const query = new URLSearchParams({
        path: filePath,
        expires: String(expires),
        signature,
    });
    return `${toolboxProxyUrl.replace(/\/+$/, '')}/${sandboxId}${operationPath}?${query.toString()}`;
}
//# sourceMappingURL=fileUrlSigning.js.map