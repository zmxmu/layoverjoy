# 03 · 已完成功能

> 权威细节记录在 `project/docs/IMPLEMENTATION_STATUS.md`（163 行，按方案分段）；本文件是模块级状态摘要。
> 每个功能都经过**真机验收**（模拟器 `emulator-5554` / Pixel_6）+ 自动化测试。

## 0. 当前质量基线（本次交接实测，2026-08-30 11:46）

| 检查 | 命令（在 `project/`） | 实测结果 |
|---|---|---|
| 后端类型 | `cd backend && npx tsc --noEmit` | **0 error** |
| 后端测试 | `cd backend && npx vitest run` | **66 passed / 6 test files**，956ms |
| Android 单测 | `cd android && ./gradlew testDebugUnitTest` | **5/5**（requiredDocuments 解析） |
| i18n 完整性 | `bash scripts/check-i18n.sh` | **✅ 308 key 全部已定义（中英双份）** |
| Android 构建 | `cd android && ./gradlew assembleDebug` | BUILD SUCCESSFUL，APK 已装到模拟器 |
| 服务健康 | `docker compose ps` | 4 服务 Up，api/db/redis healthy |
| 运行期配置 | `curl -s localhost:8080/v1/integrations` | 见 02 §7 |
| Nosana 推理 | `GET {NOSANA_OPENAI_BASE_URL}/models` | **200，0.96s** |
| Git | `git log --oneline -1` | `7932a042`，工作区干净 |

后端测试文件：`backend/test/{bookings-saga, entry-rules-v2, required-documents, rich-explanation, rule-engine, stopover-ease-score}.spec.ts`。

## 1. 模块状态一览

| 模块 | 状态 | 关键实现与验收 | commit |
|---|---|---|---|
| 首页 Home | ✅ 完成 | 从静态海报改为**漏斗入口** + **Visa-Aware 机会卡**（含 CTA 直跳搜索并预填路线）；tab 导航确定性化 + 刷新竞态导致会话丢失已修 | `35967718`/`ffa4e80a`/`b1023fcf` |
| 注册 / 登录 / 会话 | ✅ 完成 | `/v1/auth/*`；JWT + refresh 轮换；DataStore 持久化；`TokenHolder` 锁内刷新（解决重启丢会话）；语言切换入口在「我的」 | — |
| 旅行证件钱包 | ✅ 完成 | `/v1/me/documents` CRUD；单 ACTIVE 主护照 + 多签证/居留；**不存证件号码明文**（字段级 `FIELD_CRYPTO` 加密）；v2 需要的新字段已加 | `16fe5ad1` |
| 城市与机场选择器（12 号方案） | ✅ 完成 | 离线目录 schema 2.0.0；模糊搜索（中/英/拼音/首字母/别名/IATA）；洲→国家→城市三级浏览；多机场「全市/指定」；SHA/KUL/BKK 消歧；Search Request V2；断网可用 | `0eba90af` + `e3fbcae3` |
| 航班搜索与结果 | ✅ 完成 | Atlas Sandbox 真实 Search；多机场受控展开；候选 ≤3 城 × ≤2 天数；`PARTIAL` 语义；15min 缓存；结果页错误码映射（LOC-06） | `0eba90af` |
| 签证 / 入境资格判断 | ✅ 完成（v2 引擎） | 13 号方案 ER-01..15：三值逻辑、多规则版本化与原子激活/回滚、负向优先、临时政策自动失效、`/v1/entry-eligibility/assess`、结果页资格卡（徽章/停留上限/材料/依据/法律提示） | `16fe5ad1` |
| 方案详情 | ✅ 完成 | 净体验窗口**顶部唯一展示**（无模型参与）+ `experienceContext`（窗口标签/预算说明/置信度/同机场/便利度分）+ 证件区（对象数组契约，见 08） | `47825653`/`7932a042` |
| AI 中转价值解读（14 号方案） | ✅ 完成 | AI-01..12：确定性上下文 + 便利度分 + v2 结构化 Prompt + Validator + 同构模板降级 + single-flight 缓存；UI 不显示任何模型元数据（仅 `BuildConfig.DEBUG` 下 `Log.d("LayoverJoyAI", …)` 脱敏尾缀）；自动生成 + Skeleton；「重新生成」→「调整旅行偏好」 | `47825653` |
| Nosana 真实推理 | ✅ 完成 | 真实 GPU endpoint；native `/api/chat` + `think:false` 使 45s→~4s；换 `qwen2.5-3b` 轻量部署；API 风格自适应（404/405 回退 OpenAI 兼容） | `45ae6c6e`/`335d0385`/`15484d25` |
| 预订（双订单 Saga） | ✅ 完成（Order/Pay 为 MOCK） | `/v1/bookings`；资格门禁重评；两张独立单程订单；`PARTIAL_BOOKING` + 模拟退款 + `simulate-leg-b-failure` 演示路径；价格上涨需重新确认 | — |
| 价格监控 | ✅ 完成 | `MonitorRule` + `monitor-worker` 轮询 + `/v1/monitors` | — |
| 通知 | ⚠️ 部分 | 站内 `/v1/notifications` ✅；邮件真实发送 ✅（同域/内部收件人 11 SENT）；**外部域名收件人被中继拒绝**（11 FAILED，`554 5.7.1 External delivery…`）；FCM Push 未实现 | — |
| Daytona 集成 | ⚠️ 部分（决策如此） | `planning-jobs` 状态机 + 运行证据 + 清理路径齐备；默认 `local-runner`；`infra/daytona/*` 部署脚本可创建公网 Preview 实例；**`DAYTONA_MODE=live` 真沙箱未在本轮重跑** | `5ebf2ec9` |
| App 品牌 | ✅ 完成 | 显示名统一英文 `LayoverJoy` | `d29998ed` |
| 工程验收报告 | ✅ 存在 | `project/Qoder工程验收审查报告.md`（464 行） | — |

## 2. 真实 API 与 Mock 的确切边界（评委问起必须答对）

- **真实**：Atlas Sandbox Search + Verify（返回真实可交易的 Sandbox Offer 与模拟报价）、Nosana GPU 推理、大赛 SMTP 邮件、PostgreSQL/Redis 持久化与缓存、入境规则引擎（确定性计算，规则源自官方来源）。
- **Mock 且 UI 已明确标注**：Order / Pay / Refund 默认 `mock`；2026-08-30 用户授权后 Order/Pay 支持 `sandbox`（仅 Sandbox 模式 + 组合校验，默认 Profile 仍为 mock；Refund 仍锁定 `mock`）、Demo fixture 回退结果（`DEMO_FIXTURE_ENABLED`，标记 MOCK）、Atlas Webhook（`WEBHOOK_MODE=simulate`，调试路由手动触发）、Daytona 候选评估默认 `local-runner`。
- **不是 Mock**：签证/入境结论、净体验窗口、便利度分（全部确定性计算，AI 无权生成）。

## 3. AGENTS.md §11 完成门禁对照

| 门禁 | 状态 | 证据 |
|---|---|---|
| Atlas 官方测试路线 Search 通过 | ✅ | SIN→SHA / SIN→LAX 等端到端产出 KUL/BKK 方案；`BKK→SHA` 证明机场级选择生效 |
| Atlas 脱敏 Search Fixture 已存 | ⚠️ 部分 | `qoder-input/fixtures/atlas/sandbox-test-routes.json`（官方路线清单，标注 `DOCUMENTED_NOT_LIVE_VERIFIED`）；**逐次 Search 响应脱敏 Fixture 未系统保存 → 待确认是否需要在提交前补** |
| Nosana 真实 Endpoint 请求通过 | ✅ | `/models` 200；详情页 v2 卡真机渲染；单行完成日志 `provider=NOSANA` |
| Nosana 脱敏 Fixture 已存 | ❌ | `fixtures/api/` 目前只有 `plan-detail-required-documents.json` → **未满足，见 04** |
| Daytona Sandbox Job 真实运行 | ⚠️ | 历史提交 `5ebf2ec9`（Daytona 隔离评估重写）；本轮为 `local-runner`，若要声称「真实沙箱运行」需 `DAYTONA_MODE=live` 复跑留证 |
| App 中 Sandbox 标识正确 | ✅ | 「Atlas Sandbox 模拟报价」标签（`common.simulated_quote`） |
| 当前注册用户可以收到邮件 | ⚠️ 同域可达、外部域名被拒 | `NotificationDelivery`：11 SENT / 11 FAILED（见 04 KN-02） |
| 本地 Docker 回退可启动 | ✅ | `docker compose up --build` 4 服务 healthy |

## 4. 提交历史（新→旧，全部已 push 到 `origin/main`）

| commit | 内容 |
|---|---|
| `7932a042` | fix(docs-contract)：requiredDocuments 统一对象数组契约（后端归一 + Android string-or-object + UI 证件区 + 13 个测试 + 脱敏 Fixture） |
| `47825653` | feat(ai-value)：14 号方案 AI 中转价值解读（AI-01..12，15 文件 +2214 行） |
| `16fe5ad1` | feat(entry-rules)：v2 中国普通护照入境资格引擎（ER-01..15） |
| `d29998ed` | chore(android)：应用显示名改英文 LayoverJoy |
| `e3fbcae3` | fix(locations)：深层链选择丢失 / 弹回选择页 |
| `0eba90af` | feat(locations)：12 号方案城市与机场选择器（LOC-01..06） |
| `e198b700` | fix(nosana)：金额移出 AI 职责 + 输出校验（事实守卫） |
| `15484d25` | perf(nosana)：换 qwen2.5-3b 部署、API 风格自适应、事实纪律化 Prompt |
| `335d0385` | perf(nosana)：native /api/chat + think:false，45s → ~4s |
| `45ae6c6e` | feat(nosana)：真实 GPU 推理接入 + 中英双语内容 |
| `b1023fcf` | fix(android)：确定性 tab 导航 + 刷新竞态丢会话 |
| `ffa4e80a` | feat(home)：签证感知机会卡（HOME-P0） |
| 更早 | `35967718` 首页漏斗入口、`a955ab2a` i18n 检查脚本、`5ebf2ec9` Daytona 隔离评估重写、若干 `first` |
