# 本目录的 .md 交接文档是**要提交**的（里面不含任何真实密钥，只有变量名、位置与指纹）。
# 这里只拦「用户自己跑 scripts/export-env-bundle.sh 生成的明文包」，
# 防止一次性搬运产物被误提交到 Git / 推到 GitHub remote。

env-bundle.local.txt
full-env-backup.txt
*.local.txt
*.env-bundle
*.secret*
*.secrets-dump*

# 例外：本 .gitignore 自身与所有 Markdown 必须可见
!.gitignore
!*.md
