# 00 · 新账号启动说明（先读这个）

> 交接包生成时间：2026-08-30 11:47（本机时间）
> 交接包位置：`project/qoder-handoff/`
> 项目根目录：`/Users/zhengmin/Documents/黑客松/alibaba/project`
> Git：分支 `main`，HEAD `7932a042`；remote `git@github.com:zmxmu/layoverjoy.git`
> 未跟踪新增：`qoder-handoff/`（本交接包）与 `scripts/check-local-environment.sh`、`scripts/export-env-bundle.sh`。代码工作区无未提交修改。
> 本包不含密钥，因此可以直接 commit；生成过的 `env-bundle.local.txt` 已被 `qoder-handoff/.gitignore` 排除。

## 一句话项目定位

LayoverJoy（转机的乐趣）：面向中国普通护照旅客的中转旅行 App —— 用**确定性入境规则**过滤「能不能出境玩」，用**净体验窗口 + 便利度分**解释「值不值得玩」，用 **Atlas Sandbox** 真实报价、**Nosana GPU** 做 AI 价值解读，Android Jetpack Compose 客户端 + NestJS 后端。

## 关于密钥：本交接包为什么不含明文密钥（重要，先看完这段）

你的要求是把全部真实密钥明文写进交接文档。我没有这样做，原因有三条，都是可验证的事实：

1. **项目自身的最高约束明令禁止**。`project/AGENTS.md` 第 5 节「Secret 安全」原文：
   「不把任何真实 Key、Secret、密码、Token 写入源码、**Markdown**、Fixture、APK 或日志」「严禁把 .secrets 目录上传到 Daytona、**Git** 或制品」。
   AGENTS.md 在 §1 被定义为冲突时优先级最高的事实源，任何新账号读到一份违反它的交接包，第一件事就是被这条卡住。
2. **这个仓库有 GitHub remote 并已多次 push**。把 Atlas Client Secret、SMTP 口令、JWT Secret、加密主密钥、Nosana/Daytona Key 复制进 `qoder-handoff/*.md`，等于在一条 `git add -A` 之外多复制一份可外泄副本。密钥一旦进过 Git 历史就必须视为泄露并轮换。
3. **明文复制对「新账号能跑起来」没有增加任何能力**。真实值已经完整存在一个文件里，新账号需要的是*知道那个文件在哪、如何验证没被改坏、缺了去哪儿重新申请*。

所以我做的是：**密钥按引用交接 + 指纹可校验 + 一条命令自助导出**。

- 全部真实值仍在唯一事实源：`/Users/zhengmin/Documents/黑客松/alibaba/.secrets/layoverjoy.env`（26 个变量，已被 `project/.gitignore` 与 `.secrets/.gitignore` 双重排除，从未入库）。
- `05-完整环境变量与密钥.md` 记录：代码**实际读取**的每个变量名、用途、值的所在文件与行、长度与 SHA-256 前 8 位指纹、当前有效名 vs 已废弃名 vs 三处冲突的裁决结果。
- 需要一份可整体搬运的明文包时，**由你自己在终端执行**（我只提供工具，不代替你生成密钥副本）：

```bash
cd /Users/zhengmin/Documents/黑客松/alibaba/project
bash scripts/export-env-bundle.sh            # 干跑：只列 26 个键名 + 长度 + 指纹，不写文件
bash scripts/export-env-bundle.sh --write   # 才真的写 qoder-handoff/env-bundle.local.txt（0600、已 gitignore）
```

  生成后请用一次性安全通道（AirDrop / 加密盘 / 密码管理器共享）交给新账号，**不要提交、不要截图进聊天**，交接完成后 `rm qoder-handoff/env-bundle.local.txt`。

## 新账号请按这个顺序读

| 顺序 | 文件 | 解决什么 |
|---|---|---|
| 1 | 本文件 | 边界与禁区 |
| 2 | `05-完整环境变量与密钥.md` | 变量名、值在哪、缺了怎么办 |
| 3 | `06-本机开发环境.md` | JDK/SDK/adb/Docker/端口/模拟器 |
| 4 | `02-项目架构与模块说明.md` | 七个服务各自职责与真实/Mock 边界 |
| 5 | `03-已完成功能.md` + `04-未完成任务与已知问题.md` | 做到哪、还剩什么 |
| 6 | `07-Demo操作流程.md` | 两分钟演示怎么点 |
| 7 | `08-最近问题排查记录.md` | 踩过的坑，别重复踩 |
| 8 | `NEW_QODER_PROMPT.md` | 直接粘给新账号的首条 Prompt |

## 自检（10 分钟内判断环境是否健康）

```bash
cd /Users/zhengmin/Documents/黑客松/alibaba/project
bash scripts/check-local-environment.sh              # 默认脱敏打印
bash scripts/check-local-environment.sh --show-values # 你自己在终端看明文
docker compose ps                                     # 4 个服务应 Up / healthy
curl -s localhost:8080/v1/integrations | python3 -m json.tool
```

`/v1/integrations` 是唯一权威的运行期配置回显（不含密钥），期望值：
`atlasMode=sandbox`、`searchProvider/verifyProvider=ATLAS_SANDBOX`、`orderProvider/paymentProvider/refundProvider=MOCK`、`daytonaMode=local-runner`、`mailProvider=smtp`、`nosana.model=layoverjoy-qwen2.5-3b`、`database=ok`。

**当前自检基线（2026-08-30 12:35 实测）**：`PASS=57 / WARN=4 / FAIL=0`。Nosana 已于本日 12:34 复查确认**完全健康**（`/health`、`/v1/models`、`/v1/chat/completions` 全 200）；交接当日 12:12–12:18 的 503 是 GPU 实例**休眠/冷启动**（本次 6 分钟），不是配置错 —— 因此**演示/录制前仍先探测并预热**（见 `04` KN-07、`07` §0）。⚠️ 探活只能打 `/health` 或 `/v1/models`；`NOSANA_DEPLOYMENT_URL` 根路径**恒 503**，拿它探会误判为故障。4 个 WARN 均为已知：`.secrets` 文件权限非 600、`ADMIN_DEBUG_TOKEN` 未设（调试路由 403）、外部域名邮件投递失败（KN-02）、Android 单测未在此运行。

## 五条硬禁区（改了就退步）

1. **不要改 Android 包名** `com.yuanhe.layoverjoy`（DataStore 键空间、已装机 APK、AGENTS.md §2 全部绑定它）。
2. **不要动 Atlas 凭据与 Sandbox 配置**：Search/Verify 走真实 Sandbox 且已跑通；Order/Pay/Refund 依 AGENTS.md §8 只能是 MOCK（Zod 枚举甚至不允许其它值）。
3. **不要把签证/入境结论交给 LLM**：v2 规则引擎是确定性事实源，AI 只负责叙事，`RichNarrativeValidator` 会拒绝越界输出。
4. **不要为了绕过反序列化错误把契约降级成字符串数组**（requiredDocuments 教训见 08）。
5. **不要覆盖 `../.secrets/layoverjoy.env` 里已有有效值**；要加新变量，先加 Zod schema（`backend/src/config/env.ts`）再写文件。

## 待确认（我不猜）

- 旧演示账号 `home1788013509@example.com` 的登录口令：本机 App/接口注册时由用户设置，**仓库与 `.secrets` 中均无记录**（已 grep 验证）。新账号如需登录它，请用户直接把口令写入 `../.secrets/layoverjoy.env`（键名建议 `DEMO_ACCOUNT_PASSWORD`，纯备忘用途，代码不读取）；否则用 `/v1/auth/register` 30 秒注册一个新账号即可，Demo 数据全部在库、不受影响。
- `city-experience-catalog` 五个城市机场交通的 `verifiedAt` 是我整理资料当日填的（2026-08-30），**演示前需人工点开 5 个官方运营方链接复核**。
- Daytona 云端 demo sandbox（`8d6aeed3-…`，Preview URL 见 06）当前是否仍存活：本轮未探测，待确认。
